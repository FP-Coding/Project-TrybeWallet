const INITIAL_STATE = {
  currencies: [],
  expenses: [],
  editor: false,
  idToEdit: 0,
};

function user(state = INITIAL_STATE, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default user;
